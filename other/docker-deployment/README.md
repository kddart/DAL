# Go into Makefile and update the URL as needed

# Optional: use the -v flag in the docker run commmand in build.sh to map a DAL directory into docker container

# Use make run to build all you need.
